fsound by ant1

fsound.exe is 32-bit program for Microsoft Windows. It crudely emulates the PC speaker and renders songs to an 8-bit unsigned WAV file.

example.fss is an fss (fsound song) file with an explanation of the format and a short example.



Usage:

"fsound example.fss" to render example.fss to example.wav

